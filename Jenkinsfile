pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                // A kód letöltése a GitHub-ról
                checkout scm
            }
        }
        stage('Build and Test') {
            steps {
                // Futtathatsz teszteket és build lépéseket itt
            }
        }
    }
}
